# Upstream Merge Notes

The refreshed planning baseline must contain both:

- fork-specific platform work currently present on `Rilan-Dev/hermes-agent:main`;
- upstream changes through `NousResearch/hermes-agent@d604141d097eec4a49493ad1eaceb9b2ca1e496d`.

Conflicts must be resolved by preserving fork-only product work while accepting upstream runtime, gateway, provider, onboarding, tooling, dependency, and test updates unless an explicit incompatibility is documented.
